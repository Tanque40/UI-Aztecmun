<div id="contraYSoloContra">
    <div class="row">
        <div class="col s12">
            <div class="card">
                <div class="card-content">
                    <div class="row">
                        <div class="col s3">
                            <div class="row">
                                <div class="col s12">
                                    <span class="negritas">A Favor</span><br><br>
                                    <div class="input-field">
                                        <% options = @presentes.map {|s| [s.pais, s.pais]} %>
                                        <%= select_tag(:propone, options_for_select(options)) %>
                                        <%= label_tag :propone %>
                                    </div>
                                    <br><br>
                                </div>
                                <div class="col s12">
                                    <div class="input-field">
                                        <% options = @presentes.map {|s| [s.pais, s.pais]} %>
                                        <%= select_tag(:propone, options_for_select(options)) %>
                                        <%= label_tag :propone %>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="col s6">
                            <%= render :partial => "layouts/timer" %>
                        </div>
                        <div class="col s3">
                            <div class="row">
                                <div class="col s12">
                                    <span class="negritas">En contra</span><br><br>
                                    <div class="input-field">
                                        <% options = @presentes.map {|s| [s.pais, s.pais]} %>
                                        <%= select_tag(:propone, options_for_select(options)) %>
                                        <%= label_tag :propone %>
                                    </div>
                                    <br><br>
                                </div>
                                <div class="col s12">
                                    <div class="input-field">
                                        <% options = @presentes.map {|s| [s.pais, s.pais]} %>
                                        <%= select_tag(:propone, options_for_select(options)) %>
                                        <%= label_tag :propone %>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="card-action">
                    <div class="center">
                        <a href="#!" data-remote="true" onclick="votacion('<%= (esNATO?)? "Chose else Topic" : "Elegir otro Tema" %>')" class="waves-effect waves-light btn blue">Finalizar</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

</div>
